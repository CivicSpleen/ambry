documentation:
    main: '

        # An Example That Uses Other Bundles


        This example uses two other bundles as dependencies and has a few sources

        '
    readme: '# An Example That Uses Other Bundles


        This example uses two other bundles as dependencies and has a few sources'
