TITLE: A Crime Manifest

This is a fake crime file manifest

UID: m5PJKUrX0U

PARTITIONS:

example.com-random-example1-0.0.2
example.com-segmented-example-1

VIEW: doubled_int

SELECT
    *,
    2*cast("int" AS INTEGER) as doubled
FROM "pHSyDm4MNR001002_example1"

DOC:

The `doubled_int` view just doubles the int.

MVIEW: quadrupled

SELECT
    *,
    2*doubled as quadrupled
FROM doubled_int

DOC:

The `quadrupled` column of this table is twice the doubled int value.

VIEW: combined

SELECT
    quadrupled.*,
    ex.uuid as ex_uuid
FROM quadrupled
LEFT JOIN "p00G001001_example" as ex on ex.id = quadrupled.id
