
environment:
    category: development

filesystem:
    root: '/tmp/test'
    downloads: '{root}/downloads'
    extracts: '{root}/extracts'
    python: '{root}/python'
    documentation: '{root}/doc'
    build: '{root}/build'
    source: '{root}/source'
    search: '{root}/search'

library:
  database: sqlite://
  warehouse: sqlite://
  remotes:
    test: '{root}/test-remote'
    example: '{root}/example-remote'

numbers:
    host: numbers.ambry.io
    key: fe78d179-8e61-4cc5-ba7b-263d8d3602b9
    port: 80

