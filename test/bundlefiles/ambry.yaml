
environment:
    category: development

filesystem:
    root: '/tmp/test'
    downloads: '{root}/downloads'
    extracts: '{root}/extracts'
    python: '{root}/python'
    documentation: '{root}/doc'
    build: '{root}/build'
    source: '{root}/source'
    search: '{root}/search'

library:
  database: test-database
  warehouse: sqlite://
  remotes:
    test: 's3://test.library.civicknowledge.com/devel'
    test-orig: '{root}/test-remote'
    example: '{root}/example-remote'

database:

  test-database: sqlite:////tmp/test.db
  postgres-test: postgres://user:pass@host # Do not include db name here because db will be created while tests running

  pg-func-test: postgres://ambry10@warehouse2/ambry10-test

  database1: sqlite:////foo/bar
  database2: postgres://user:pass@host/dbname
  database3:
    driver: postgres
    username: user
    password: pass
    server: host
    dbname: dbname
  database4:
    driver: postgres
    server: host1
    dbname: dbname
  database5:
    driver: postgres
    server: host2
    username: user2
    dbname: dbname


services:
    numbers:
        self-key:
        unregistered-key: fe78d179-8e61-4cc5-ba7b-263d8d3602b9
        host: numbers.ambry.io
        port: 80

