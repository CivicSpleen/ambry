
library:
    default:
        filesystem: default
        database: default
        remotes:
        - 'http://library.civicknowledge.org'

    server:
        port: 7979
        host: localhost
        filesystem: default
        database: default

database:
    default:
        dbname: '{root}/library.db'
        driver: sqlite

filesystem:
    root: /data
    
    default:
        dir: '{root}/library'

    downloads:
        dir: '{root}/cache/downloads'

    extracts: 
        dir: '{root}/cache/extracts'

    python: '{root}/cache/python'

sourcerepo:
    dir: '{root}/source'

    default:
        account: github-account-name
        service: github
        org: clarinova-data

numbers:
    host: numbers.ambry.io
    port: 80
    key: fe78d179-8e61-4cc5-ba7b-263d8d3602b9

repository:
    default:
        url: <ckan-api-url>
        key: <ckan-key>
